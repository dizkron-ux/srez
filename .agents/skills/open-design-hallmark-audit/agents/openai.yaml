interface:
  display_name: "OpenDesign Hallmark Audit"
  short_description: "SREZ-safe anti-slop and visual QA"
  default_prompt: "Use $open-design-hallmark-audit to audit the requested SREZ UI without changing product structure."
policy:
  allow_implicit_invocation: false
